# dalya-post-closeout-hardening - Work Plan

## TL;DR (For humans)
**What you'll get:** A five-ticket follow-up plan that cleans the just-finished closeout state, repairs backend runtime QA, captures the final browser surface proof safely, closes the seller conversation summary PII gap, and adds a rationale-backed guard against Verified Facts rule-key drift.

**Why this approach:** It starts with evidence/tooling cleanup so future product fixes stop fighting the same broken test environment, then handles the privacy/security item before a small integrity guard. The only planned product behavior change is seller-visible PII redaction; Task 5 may make the smallest output-gate typo correction only if the new integrity test exposes one.

**What it will NOT do:** It will not run Task 10b, touch production/staging data or RLS, enable Telegram/360dialog/BSP, modify dependency pins, or broaden the product surface.

**Effort:** Medium
**Risk:** Medium - one seller-visible API behavior change plus QA/tooling work around env/dependency guardrails.
**Decisions to sanity-check:** Use a local Python 3.12/3.13 path rather than changing dependencies; run F3 through a safe no-secret frontend launch path; keep seller redaction server-side and preserve agent-facing summaries.

Your next move: approve this plan for `$omo:start-work`, or ask for a high-accuracy review first. Full execution detail follows below.

---

> TL;DR (machine): Medium-risk sequential post-closeout hardening plan: verification closeout cleanup, runtime QA environment repair, safe F3 browser QA, seller conversation PII redaction, Verified Facts rule-key integrity; Task 10b excluded.

## Scope
### Must have
- Five sequential follow-up tickets, each with one branch, one PR, merge guard, merge, and cleanup before the next starts.
- Verification closeout cleanup: final-wave checklist state repaired or explicitly documented; closeout report updated with PR #67 merge commit if needed; scope guard tightened for audit/migrate helper-script drift; accepted merge-gate overrides documented, including PR #59 and PR #62 if their merge guards emitted BLOCKED but the PRs merged.
- Runtime QA repair: healthy Python 3.12/3.13 local venv path for focused no-DB backend QA, or a deterministic BLOCKED artifact proving no compatible interpreter exists; canonical test commands documented. Runtime QA must not be claimed green unless tests actually ran.
- Final surface QA pass: safe browser verifier run with screenshots/logs for `/agent`, Today Queue escalation focus, dashboard fetch-failure state, escalation inbox reply affordance, and first-run empty/error states; safe temp workdir excludes env files, `.next`, caches, and local build artifacts.
- Seller conversation summary PII protection: seller-visible response copies redact buyer names, phones, WhatsApp handles, direct conversation IDs, and direct identifiers while preserving stored data and agent-facing summaries.
- Verified Facts rule-key integrity: every output-gate `fact_key` resolves to seed data or is explicitly marked expected-missing/defer-only with a short rationale.
### Must NOT have (guardrails, anti-slop, scope boundaries)
- No Task 10b production RLS/app-role rollout.
- No production/staging DDL, migrations, RLS enablement, role/grant mutation, live writes, external DB-backed tests, or production/staging env-file content reads.
- No dependency or lockfile edits.
- No product behavior changes in Tasks 1, 2, 3, or 5 except a typo fix if Task 5 exposes one.
- No agent-facing summary redaction in Task 4.
- No Telegram replacement integration, 360dialog/BSP enablement, owner-dashboard expansion, broad CRM work, or unrelated docs churn.

## Verification strategy
> Verification should be agent-executed where safe. If dependency repair or browser launch would touch production/staging envs, external DBs, live writes, migrations, RLS, or unapproved SaaS writes, stop and mark the exact command blocked.
- Test decision: TDD for Task 4 and Task 5; tests-after/static verifier updates for Tasks 1-3 because they are closeout/tooling verification tickets.
- Backend framework: focused no-DB `pytest` only through a healthy local Python 3.12/3.13 venv; if unavailable, record BLOCKED evidence rather than inventing a CI fallback.
- Frontend framework: Node verifier scripts, Playwright screenshots/logs, and `npx --no-install tsc --noEmit`.
- Static gates for every task: `git diff --check`, conflict-marker scan, forbidden-scope scan, and explicit no-Task-10b confirmation.
- Evidence: `.omo/evidence/task-<N>-dalya-post-closeout-hardening.*`.

## Execution strategy
### Parallel execution waves
> Target 5-8 todos per wave. Fewer than 3 (except the final) means you under-split.
- Eric requested ordered next actions. Execute sequentially despite conceptual independence: one ticket, one branch, one PR, merge guard, merge, cleanup, then the next.
- Create one generic Linear ticket per task before implementation unless Linear is unavailable or Eric explicitly switches to local-only tracking for that task. Public titles: "Clean up readiness verification closeout", "Repair focused runtime QA path", "Capture final surface QA evidence", "Sanitize seller conversation summaries", and "Verify Verified Facts rule keys". If Linear is unavailable, create `.omo/evidence/task-<N>-tracking-dalya-post-closeout-hardening.json` with the same public title and record the failure reason.
- Public PR titles stay generic and contain no security-sensitive details.
- Tasks 1-3 are tooling/evidence cleanup and may be reviewed together conceptually, but must still be implemented as separate PRs.

### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1 Verification closeout PR | finished next-MVP plan | 2, 3, final | none in execution |
| 2 Runtime QA environment repair | 1 | 3, 4, 5, final | none in execution |
| 3 Final surface QA pass | 1, 2 | 4, 5, final | none in execution |
| 4 Seller conversation summary PII | 1, 2, 3 | 5, final | none in execution |
| 5 Verified Facts rule-key integrity | 1, 2, 3, 4 | final | none in execution |

## Todos
> Implementation + Test = ONE todo. Never separate.
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->
- [x] 1. Verification closeout PR.
  What to do / Must NOT do: Repair or explicitly document the final closeout checklist state: F1/F2/F4 passed, F3 blocked or rerun; update the closeout report with PR #67 merge commit `ddae7bb231e4e5c2d3f3353010d6113bd54d0aab` if the report still omits it; tighten the scope guard so it fails or requires explicit allowlisting for edits under `scripts/migrate_*.py` and `scripts/audit_*.py`; add a short note explaining accepted merge-gate overrides such as blocked backend pytest, blocked final surface smoke, and env-file guarded build. Document PR #59 and PR #62 specifically if their merge guards emitted BLOCKED but the PRs were merged anyway; classify each as accepted residual risk with Eric approval, stale/advisory gate, or process error. Future gates must use PASS / BLOCKED / ADVISORY consistently. Do not change product behavior.
  Parallelization: Wave 1 | Blocked by: none | Blocks: 2, 3, final
  Branch/PR: `codex/verification-closeout-cleanup`; public PR title `Clean up readiness verification closeout`.
  References (executor has NO interview context - be exhaustive): `.omo/plans/dalya-next-mvp-readiness-plan.md:219`; `reports/dalya-next-mvp-readiness-closeout-2026-06-23.md:12`; `.omo/evidence/task-13-next-mvp-readiness-closeout.md:30`; `scripts/verify_next_mvp_scope_guard.py:16`; `BACKLOG.md:159`; `PROJECT_BRIEF.md:67`.
  Acceptance criteria (agent-executable): final checklist or evidence text unambiguously states F1/F2/F4 PASS and F3 BLOCKED or PASS; closeout report names PR #67 and merge `ddae7bb`; PR #59 and PR #62 merge-guard histories are explicitly documented if either had a BLOCKED guard at merge time, with each classified as accepted residual risk with Eric approval, stale/advisory gate, or process error; scope guard flags unapproved audit/migrate helper-script changes while allowing the known Task 13 separator-only edits through an explicit allowlist or rationale file; report/backlog/brief remain aligned and keep Task 10b blocked.
  QA scenarios (name the exact tool + invocation): RED `python3 -m pytest --noconftest tests/test_next_mvp_scope_guard_helper_scripts.py -q` fails before the scope guard supports strict helper-script detection. The test must write fixtures under `/private/tmp/dalya-scope-guard-fixtures/changed-paths.txt` and `/private/tmp/dalya-scope-guard-fixtures/diff.patch` containing `scripts/migrate_fake.py` and `scripts/audit_fake.py`, then invoke `python3 scripts/verify_next_mvp_scope_guard.py --base 3482a7fb863c542836fa2aabef707ad8fd503b71 --output /private/tmp/dalya-scope-guard-fixtures/out.json --changed-paths-fixture /private/tmp/dalya-scope-guard-fixtures/changed-paths.txt --diff-fixture /private/tmp/dalya-scope-guard-fixtures/diff.patch --helper-script-allowlist .omo/evidence/task-1-helper-script-allowlist.json`; expected observable: exit non-zero and JSON result naming unapproved helper-script edits. GREEN same pytest passes after implementing the exact fixture flags and allowlist behavior; direct GREEN `python3 scripts/verify_next_mvp_scope_guard.py --base 3482a7fb863c542836fa2aabef707ad8fd503b71 --output .omo/evidence/task-1-scope-guard-green.json --helper-script-allowlist .omo/evidence/task-1-helper-script-allowlist.json`; closeout consistency `python3 scripts/verify_next_mvp_plan_completion.py --plan .omo/plans/dalya-next-mvp-readiness-plan.md --evidence-dir .omo/evidence --output .omo/evidence/task-1-plan-compliance.json`; negative claim scan `! rg -n "production ready|live data ready|external brokerage pilot ready|Task 10b.*complete|Telegram.*enabled|360dialog.*ready|BSP.*ready" reports/dalya-next-mvp-readiness-closeout-2026-06-23.md PROJECT_BRIEF.md BACKLOG.md docs/runbooks/dalya-friendly-pilot-readiness-runbook.md`; static `python3 -m py_compile scripts/verify_next_mvp_scope_guard.py tests/test_next_mvp_scope_guard_helper_scripts.py`; `git diff --check`; conflict scan `rg -n "<<<<<<<|=======|>>>>>>>" .omo/plans/dalya-next-mvp-readiness-plan.md reports BACKLOG.md PROJECT_BRIEF.md docs/runbooks scripts tests`.
  Evidence: `.omo/evidence/task-1-dalya-post-closeout-hardening.md`.
  Merge guard: docs/scripts/evidence scope only; no product behavior files; no Task 10b; no env-file reads; no dependency/lockfile edits.
  Commit: Y | `docs(readiness): clean up verification closeout`

- [x] 2. Runtime QA environment repair.
  What to do / Must NOT do: Establish a healthy local Python 3.12/3.13 path for focused no-DB backend QA. Implement a small runner script such as `scripts/run_focused_backend_qa.py` plus a docs/runbook wrapper that creates `/private/tmp/dalya-focused-backend-qa-venv`, installs from existing pinned repo dependency files only, runs the final focused backend suite, records output, and cleans up. Do not create `.github/workflows` in this ticket; if no Python 3.12/3.13 interpreter exists locally, the ticket may still merge with the runner and canonical commands plus a deterministic BLOCKED evidence artifact proving no compatible interpreter exists, but it must not claim runtime QA green. Do not touch app behavior, product code, dependency pins, lockfiles, production/staging envs, or DB-backed tests.
  Parallelization: Wave 1 | Blocked by: 1 | Blocks: 3, 4, 5, final
  Branch/PR: `codex/runtime-qa-env-repair`; public PR title `Repair focused runtime QA path`.
  References: `reports/dalya-next-mvp-readiness-closeout-2026-06-23.md:49`; `.omo/evidence/task-13-next-mvp-readiness-closeout.md:46`; `requirements.txt`; `tests/test_legacy_telegram_removed.py`; `tests/test_cors_live_env.py`; `tests/test_seller_lead_privacy.py`; `tests/test_verified_facts_output_gate.py`; `tests/test_verified_facts_seed_closing_costs.py`; `tests/test_needs_reply_priority.py`; `BACKLOG.md:151`; `BACKLOG.md:156`.
  Acceptance criteria (agent-executable): this task succeeds if it either produces a working focused backend QA command using Python 3.12/3.13, or produces a deterministic BLOCKED artifact proving no compatible interpreter exists. It must not claim runtime QA green unless tests actually ran. The command refuses Python 3.14 for pinned deps unless an explicit compatibility path is known; installs only from existing repo dependency files; runs the final focused backend suite with `--noconftest`; writes evidence and cleanup receipt; and invents no CI fallback.
  QA scenarios (name the exact tool + invocation): Failure proof `python3 scripts/run_focused_backend_qa.py --check-only --evidence .omo/evidence/task-2-runtime-qa-check-red.json` must report current local repo venv/Python mismatch before the repair path is used; GREEN `python3 scripts/run_focused_backend_qa.py --python python3.12 --evidence .omo/evidence/task-2-runtime-qa-green.json -- tests/test_legacy_telegram_removed.py tests/test_cors_live_env.py tests/test_seller_lead_privacy.py tests/test_verified_facts_output_gate.py tests/test_verified_facts_seed_closing_costs.py tests/test_needs_reply_priority.py -q` if Python 3.12/3.13 is available; fallback `python3 scripts/run_focused_backend_qa.py --check-only --evidence .omo/evidence/task-2-runtime-qa-blocked.json` may be BLOCKED only with exact missing interpreter/dependency reason; static `python3 -m py_compile scripts/run_focused_backend_qa.py`; `git diff --check`; conflict scan `rg -n "<<<<<<<|=======|>>>>>>>" scripts docs`.
  Evidence: `.omo/evidence/task-2-dalya-post-closeout-hardening.md`.
  Merge guard: tooling/docs only; no app source behavior; no dependency/lockfile edits; no production/staging env reads; no DB writes; temp venv removed or BLOCKED evidence recorded.
  Commit: Y | `chore(test): add focused backend QA runner`

- [x] 3. Final surface QA pass.
  What to do / Must NOT do: Run the F3 browser surface verifier in a safe env and capture screenshots/logs for `/agent`, Today Queue escalation focus, dashboard fetch-failure state, escalation inbox reply affordance, and first-run empty/error states. If `frontend/.env.local` exists, do not let Next read it. Extend `frontend/scripts/verify-next-mvp-final-surface.mjs` with `--safe-temp-workdir <path>`; in that mode it must copy or link the frontend app into the temp workdir while excluding every `.env*` file, `.next`, caches, and local build artifacts, start Next with `cwd=<safe-temp-workdir>`, record `serverCwd`, explicit safe env vars, `.env*` scan results, and cleanup receipt in the transcript. It may link/copy source and use existing installed dependencies only. Do not run `npm install`, modify package or lock files, or weaken the env-file guard.
  Parallelization: Wave 1 | Blocked by: 1, 2 | Blocks: 4, 5, final
  Branch/PR: `codex/final-surface-qa-pass`; public PR title `Capture final surface QA evidence`.
  References: `frontend/scripts/verify-next-mvp-final-surface.mjs:1`; `.omo/evidence/task-13-next-mvp-readiness-closeout.md:42`; `.omo/evidence/final-next-mvp-surface/BLOCKED.md`; `frontend/scripts/verify-task-next-escalation-route-visual.mjs`; `frontend/scripts/verify-task-next-dashboard-fallback-visual.mjs`; `frontend/scripts/verify-task-next-first-run-visual.mjs`; `frontend/src/app/(app)/agent/page.tsx`; `frontend/src/app/(app)/agent/escalations/page.tsx`; `frontend/src/components/agent-dashboard/DashboardConnectionErrorState.tsx`; `frontend/src/components/agent-dashboard/DayIsClear.tsx`.
  Acceptance criteria (agent-executable): final surface verifier produces PASS, not BLOCKED; output directory contains `agent.png`, `today-queue-escalation-focus.png`, `dashboard-fetch-failure.png`, `escalation-reply-affordance.png`, `first-run-empty-state.png`, and `first-run-error-state.png`; transcript records exact base URL, `serverCwd`, env safety mode, `.env*` scan result, DOM assertions, and cleanup receipt; DOM assertions include Today Queue focused escalation text, fetch-failure retry/manual fallback text, escalation reply affordance text or disabled-state reason, first-run safe synthetic/internal activation text, and error-state fallback text; no screenshot shows fake live operational data in authenticated failure/empty states; env-file content is not read.
  QA scenarios (name the exact tool + invocation): RED create deterministic disposable fixture `mkdir -p /private/tmp/dalya-final-surface-red/frontend && printf 'SECRET=do-not-read\n' > /private/tmp/dalya-final-surface-red/frontend/.env.local`, then run `cd frontend && node scripts/verify-next-mvp-final-surface.mjs --fixture-frontend-root /private/tmp/dalya-final-surface-red/frontend --base-url http://127.0.0.1:3999 --output-dir ../.omo/evidence/task-3-final-surface-red`; expected observable: BLOCKED because fixture `.env.local` exists and no safe temp workdir was supplied, without reading file contents. The verifier must never delete caller-owned `--fixture-frontend-root`; after recording RED evidence, remove the disposable fixture with `rm -rf /private/tmp/dalya-final-surface-red` and record that cleanup in Task 3 evidence. GREEN `cd frontend && node scripts/verify-next-mvp-final-surface.mjs --safe-temp-workdir /private/tmp/dalya-final-surface-frontend --base-url http://127.0.0.1:3000 --output-dir ../.omo/evidence/task-3-final-surface` captures the six named screenshots and transcript assertions above; type/static `cd frontend && node --check scripts/verify-next-mvp-final-surface.mjs && npx --no-install tsc --noEmit`; cleanup `test ! -d /private/tmp/dalya-final-surface-frontend && test ! -d /private/tmp/dalya-final-surface-red`; `git diff --check`; conflict scan `rg -n "<<<<<<<|=======|>>>>>>>" frontend/scripts .omo/evidence`.
  Evidence: `.omo/evidence/task-3-dalya-post-closeout-hardening.md`.
  Merge guard: verifier/evidence/docs only; no product behavior change; no production/staging env reads; no hidden dev server left running; no dependency/lockfile edits.
  Commit: Y | `test(frontend): capture final surface QA`

- [ ] 4. Seller conversation summary PII.
  What to do / Must NOT do: Sanitize seller-visible `ai_summary` values returned by `GET /api/v1/seller/listings/{listing_id}/conversations`. Redact buyer names, phone numbers, WhatsApp handles, direct conversation IDs, emails, and direct identifiers from structured fields such as `topics`, `key_question`, `next_step_hint`, `buyer_context`, `_fallback`, and any string/list/dict nested under `summary`. Preserve the existing anonymized `buyer_label` numbering and agent-facing conversation summaries. Sanitize only the seller-visible response serialization copy. Do not mutate stored `ai_summary`, conversation rows, seller `/leads` aggregation already anonymized, agent dashboard/inbox summaries, or raw stored conversation data.
  Parallelization: Wave 2 | Blocked by: 1, 2, 3 | Blocks: 5, final
  Branch/PR: `codex/sanitize-seller-conversation-summary`; public PR title `Sanitize seller conversation summaries`.
  References: `app/api/seller.py:7`; `app/api/seller.py:213`; `app/api/seller.py:244`; `app/api/seller.py:259`; `frontend/src/lib/queries.ts:327`; `tests/test_seller_lead_privacy.py:145`; `tests/test_agent_dashboard_empty_state.py`; `app/models/db_models.py:369`.
  Acceptance criteria (agent-executable): seller `/conversations` response never contains exact seeded PII strings: `conv.buyer_name`, `conv.buyer_phone`, `conv.conversation_id`, `sara@example.com`, `wa.me/971501112222`, `whatsapp:+971501112222`, `+971 50 111 2222`, `971501112222`, or seeded long token `conv_01HPIISECRET1234567890`; nested summaries are sanitized recursively on the response copy using placeholders `[redacted buyer]`, `[redacted phone]`, `[redacted email]`, `[redacted whatsapp]`, and `[redacted id]` without dropping safe context like "wants a viewing tomorrow"; tests prove stored `ai_summary` and conversation rows remain unchanged after serialization; agent-facing conversation/detail or dashboard APIs still expose buyer identity according to existing permissions; unauthorized seller/listing access remains unchanged.
  QA scenarios (name the exact tool + invocation): RED `PYTHONPATH=. python3 -m pytest --noconftest tests/test_seller_conversation_summary_privacy.py -q` fails before sanitization by seeding `conv.ai_summary` with buyer name, phone, WhatsApp handle, email, and conversation ID; GREEN same passes after fix; regression `PYTHONPATH=. python3 -m pytest --noconftest tests/test_seller_lead_privacy.py tests/test_seller_conversation_summary_privacy.py tests/test_needs_reply_signal_dal170e5.py -q` includes an exact authorized-agent assertion that `/api/v1/agent/dashboard` or `/api/v1/agent/conversations/{conversation_id}` still shows buyer name/phone where current permissions allow; if runtime pytest is unavailable, use Task 2 runner `python3 scripts/run_focused_backend_qa.py --evidence .omo/evidence/task-4-seller-summary-privacy.json -- tests/test_seller_lead_privacy.py tests/test_seller_conversation_summary_privacy.py -q`; static `python3 -m py_compile app/api/seller.py tests/test_seller_conversation_summary_privacy.py`; real-surface API proof `PYTHONPATH=. python3 scripts/verify_seller_conversation_summary_privacy.py --evidence .omo/evidence/task-4-seller-summary-api.json`, where the script seeds TestClient data, calls `/api/v1/seller/listings/{listing_id}/conversations`, asserts no seeded PII substrings, asserts safe context remains, and writes JSON evidence; `git diff --check`; conflict scan `rg -n "<<<<<<<|=======|>>>>>>>" app/api/seller.py tests scripts`.
  Evidence: `.omo/evidence/task-4-dalya-post-closeout-hardening.md`.
  Merge guard: privacy/security scope only; no agent-facing PII removal; no DB schema/migration; no production/staging env reads; no external DB tests/live writes.
  Commit: Y | `fix(seller): redact conversation summary pii`

- [ ] 5. Verified Facts rule-key integrity.
  What to do / Must NOT do: Add a small integrity test/helper proving every non-null `fact_key` in `app/core/verified_facts_output_rules.py::CLAIM_RULES` resolves to a seed fact in `app/core/data/verified_facts_seed.json` or is explicitly listed in a new constant `EXPECTED_MISSING_OUTPUT_GATE_FACT_KEYS` in `app/core/verified_facts_output_rules.py` with fields `reason` and `runtime_policy` set to `defer_only`. Each expected-missing key must include a short comment or structured reason explaining why it is intentionally defer-only and not a seed omission bug. `fact_key=None` rules must be treated as explicit defer-only. Do not change behavior unless the new test finds an actual typo; if a typo is found, fix only that key or add the explicit expected-missing annotation.
  Parallelization: Wave 2 | Blocked by: 1, 2, 3, 4 | Blocks: final
  Branch/PR: `codex/verified-facts-rule-key-integrity`; public PR title `Verify Verified Facts rule keys`.
  References: `app/core/verified_facts_output_rules.py:16`; `app/core/verified_facts_output_rules.py:32`; `app/core/verified_facts_output_gate.py:113`; `app/core/verified_facts.py:274`; `app/core/data/verified_facts_seed.json`; `tests/test_verified_facts_output_gate.py`; `tests/test_verified_facts_seed_closing_costs.py`.
  Acceptance criteria (agent-executable): integrity helper rejects a monkeypatched bogus non-null `OutputClaimRule(fact_key="missing_typo")`; current rule set passes only if every non-null key is present in seed data or explicitly listed in `EXPECTED_MISSING_OUTPUT_GATE_FACT_KEYS` with non-empty `reason` explaining why it is intentionally defer-only rather than a seed omission bug, and `runtime_policy == "defer_only"`; `fact_key=None` rules are asserted as defer-only and do not require seed facts; no prompt/output behavior changes unless correcting a typo.
  QA scenarios (name the exact tool + invocation): RED `PYTHONPATH=. python3 -m pytest --noconftest tests/test_verified_facts_rule_key_integrity.py -q` fails before the helper/expected-missing policy exists. The final test suite must include a passing test named like `test_integrity_helper_rejects_bogus_fact_key` that calls the helper with a monkeypatched bogus rule and asserts it raises or returns a failure object; it must not leave any intentionally failing test in the suite. GREEN same pytest passes for current rules; regression `PYTHONPATH=. python3 -m pytest --noconftest tests/test_verified_facts_output_gate.py tests/test_verified_facts_seed_closing_costs.py tests/test_verified_facts_rule_key_integrity.py -q`; static `python3 -m py_compile app/core/verified_facts_output_rules.py tests/test_verified_facts_rule_key_integrity.py`; optional helper `PYTHONPATH=. python3 scripts/verify_verified_facts_rule_keys.py --evidence .omo/evidence/task-5-rule-key-integrity.json`; `git diff --check`; conflict scan `rg -n "<<<<<<<|=======|>>>>>>>" app/core tests scripts`.
  Evidence: `.omo/evidence/task-5-dalya-post-closeout-hardening.md`.
  Merge guard: test/helper only unless a typo is found; no expansion of direct-safe facts; no dependency/lockfile edits; no product behavior change beyond typo correction.
  Commit: Y | `test(facts): verify output rule fact keys`

## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE. Surface results and wait for the user's explicit okay before declaring complete.
- [ ] F1. Plan compliance audit: run `git log --oneline --first-parent -15 > .omo/evidence/final-post-closeout-first-parent-log.txt`, `git status --short --branch > .omo/evidence/final-post-closeout-git-status.txt`, and `python3 scripts/verify_next_mvp_plan_completion.py --plan .omo/plans/dalya-post-closeout-hardening.md --evidence-dir .omo/evidence --output .omo/evidence/final-post-closeout-plan-compliance.json` after extending or wrapping the helper for this plan. Expected: every todo has merged PR, merge commit, cleanup receipt, evidence artifact, and no pending branch/worktree changes except expected local `.omo` evidence.
- [ ] F2. Code quality review: run `python3 scripts/review_next_mvp_final_diff.py --base ddae7bb231e4e5c2d3f3353010d6113bd54d0aab --output .omo/evidence/final-post-closeout-code-review.md` or a new post-closeout equivalent. Expected: PASS for narrow scope, no product behavior changes outside seller PII and any Task 5 typo-only correction, no dependency/lockfile edits, no test weakening.
- [ ] F3. Real manual QA: run `cd frontend && node scripts/verify-next-mvp-final-surface.mjs --safe-temp-workdir /private/tmp/dalya-final-surface-frontend --base-url http://127.0.0.1:3000 --output-dir ../.omo/evidence/final-post-closeout-surface`. Expected: PASS plus screenshots/logs for all required surfaces and cleanup receipt; if blocked, exact command and reason must be recorded, but the plan cannot be declared complete until Eric accepts the block.
- [ ] F4. Scope fidelity: run `python3 scripts/verify_next_mvp_scope_guard.py --base ddae7bb231e4e5c2d3f3353010d6113bd54d0aab --output .omo/evidence/final-post-closeout-scope-guard.json`. Expected: PASS confirming no Task 10b, production/staging DDL, migrations, RLS enablement, role/grant mutation, live writes, production/staging env reads, external DB tests, dependency/lockfile edits, Telegram replacement integration, owner-dashboard expansion, or unapproved audit/migrate helper-script behavior changes.

## Commit strategy
- Every todo is a separate branch and PR.
- Conventional commits only, one logical change per task.
- Public PR titles stay generic and accurate.
- Each PR body must include validation results and explicitly name blocked commands if any.
- Do not commit local temp venvs, screenshots outside intended evidence paths, env files, generated caches, or unrelated `.omo` noise.
- Merge only after code review and merge guard are green; delete local/remote branch before starting the next todo.

## Success criteria
- `.omo/plans/dalya-post-closeout-hardening.md` is decision-complete and executable without further interview.
- The final closeout state is internally consistent, including PR #67 merge receipt and final-wave status.
- Focused backend runtime QA has a healthy Python 3.12/3.13 path, or exact BLOCKED evidence if no local compatible interpreter is available.
- F3 browser surface QA produces safe screenshots/logs without reading local secrets.
- Seller conversation summaries are PII-safe for sellers and unchanged for agent-facing surfaces.
- Verified Facts output-rule keys cannot silently drift from seed facts.
- Task 10b remains unexecuted and explicitly blocked.
