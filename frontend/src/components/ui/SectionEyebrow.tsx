export function SectionEyebrow({ children }: { children: React.ReactNode }) {
  return (
    <span className="text-[var(--color-brand-600,#324B6B)] text-[11px] font-semibold tracking-[0.3em] uppercase mb-4 block">
      {children}
    </span>
  )
}
